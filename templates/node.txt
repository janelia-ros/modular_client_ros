#!/usr/bin/env python

from __future__ import division, print_function
import rospy
from std_msgs.msg import String
from modular_client_ros.srv import *

from modular_client import ModularClient


dev = ModularClient()

def handle_play_tone(request):
    print(request)
    response = dev.play_tone(request.frequency,request.speaker)
    return PlayToneResponse()

def {{ nodename }}():
    rospy.init_node('{{ nodename }}')
    s = rospy.Service('~play_tone', PlayTone, handle_play_tone)
    print("{{ nodename }} running.")
    rospy.spin()

if __name__ == "__main__":
    {{ nodename }}()

